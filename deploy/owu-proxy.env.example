# The app reuses the browser password as X-OWU-Tunnel-Key. Keep this file 0600.
OWU_TUNNEL_KEY=replace-with-the-browser-password

# Resource IDs are fixed server-side. The client never supplies a host or port.
OWU_TCP_RESOURCES=ssh=127.0.0.1:22,minecraft=127.0.0.1:25565
