# Include this file from nginx.conf inside the `http {}` block.
# It is intentionally separate from the normal OWU server profile so Basic
# Auth deployments remain unchanged until SSO is explicitly enabled.
upstream owu_sso_backend {
    server 127.0.0.1:4180;
    keepalive 16;
}
